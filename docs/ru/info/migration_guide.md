<h1 align="center">🧭 Руководство по миграции XC_VM</h1>

<p align="center">
  Легко переходите с совместимых систем IPTV с помощью встроенного инструмента миграции XC_VM.
</p>

---

## 📚 Навигация

* [⚙️ Перед началом](#перед-началом)
* [🚀 Шаги миграции](#шаги-миграции)
* [🔑 Восстановление доступа после миграции](#восстановление-доступа-после-миграции)
* [🧩 После миграции](#после-миграции)
* [🖥️ Подготовка балансировщика нагрузки](#подготовка-балансировщика-нагрузки)

---

## ⚙️ Перед началом

> 💡 **Рекомендация:**
> Лучше всего выполнять миграцию на **чистой установке XC_VM**.

> ⚠️ **Важно:**
> **Настройки системы и панели НЕ переносятся при миграции.**
> Переносятся только данные базы данных, поддерживаемые процессом миграции.
> После завершения миграции все настройки необходимо **выполнить вручную**.

Если вы решите использовать **существующую установку**, обратите внимание на следующее:

* XC_VM **удалит все таблицы** в вашей основной базе данных, которые совпадают с данными из базы данных миграции.
* Всегда **создавайте резервные копии** перед выполнением миграции.

---

## 🚀 Шаги миграции

### 1. Загрузка резервной копии

Загрузите существующую резервную копию базы данных на сервер XC_VM через **SFTP**.
В этом примере предполагается, что файл находится по пути:

```
/tmp/backup.sql
```

### 2. Восстановление резервной копии в базу данных миграции

Используйте инструмент миграции для очистки существующей базы данных миграции и восстановления резервной копии:

```bash
/home/xc_vm/tools migration "/tmp/backup.sql"
```

### 3. Запуск миграции

Если восстановление завершилось без ошибок, вы можете начать миграцию одним из двух способов:

#### 🧩 Вариант 1 — Командная строка (рекомендуется)

Запустите миграцию вручную:

```bash
/home/xc_vm/bin/php/bin/php /home/xc_vm/includes/cli/migrate.php
```

#### 🌐 Вариант 2 — Веб-установщик

Если вы не хотите использовать командную строку, вы можете вернуться в **веб-установщик** по ссылке, предоставленной во время установки панели.
Выберите пункт **«Миграция»** и следуйте инструкциям на экране для интерактивного завершения процесса.

Во время миграции вы будете видеть обновления прогресса в режиме реального времени.
После завершения вы сможете войти в систему.

---

## 🔑 Восстановление доступа после миграции

Если вы не можете войти в систему (например, из-за отсутствия имени пользователя, пароля или недействительного кода доступа), используйте **инструменты восстановления**:

### Создание кода аварийного доступа

```bash
/home/xc_vm/tools access
```

### Создание администратора для восстановления доступа

```bash
/home/xc_vm/tools user
```

> ⚠️ После входа в систему немедленно **измените код доступа и учетные данные администратора**.

---

## 🧩 После миграции

* Балансировщики нагрузки по умолчанию будут отображаться как **отключенные**.
* Все потоки будут **автоматически остановлены** в целях безопасности.
* **Все настройки системы, панели и серверов должны быть настроены заново вручную.**

### Следующие шаги

1. Переустановите балансировщики нагрузки и **вручную запустите потоки** после подтверждения корректности миграции.
2. Просмотрите и обновите **настройки XC_VM по умолчанию**.
3. Проверьте данные **основного сервера** — домены, конфигурацию SSL и другие параметры.
4. Убедитесь, что конфигурация соответствует вашей инфраструктуре и требованиям.

---

## 🖥️ Подготовка балансировщика нагрузки

Необходимо выполнить переустановку операционной системы на всех балансировщиках нагрузки.

---